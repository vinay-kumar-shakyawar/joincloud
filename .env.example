# JoinCloud – Control Plane & telemetry (local testing)
# Copy to .env and adjust. Used by server/index.js and Electron (when set before spawn).

# Admin Control Plane URL. Use http:// for local testing (Admin runs on port 5000).
# When set, the app will: register host, send heartbeats, fetch config, sync telemetry, send usage aggregates.
JOINCLOUD_ADMIN_HOST=http://localhost:5000

# Alternative: full URL (same effect)
# JOINCLOUD_CONTROL_PLANE_URL=http://localhost:5000

# Optional: override storage root (default: App Support / JoinCloud)
# JOINCLOUD_STORAGE_ROOT=/path/to/storage
